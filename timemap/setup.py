from distutils.core import setup

setup(
    name = 'TimeMap',
    packages = ['TimeMap'], # this must be the same as the name above
    version = '0.1',
    description = 'Library to handle and sync task data between different systems',
    author = 'Ryan McCarl',
    author_email = 'admin@wordbrewery.com',
    url = 'https://github.com/RyanMcCarl/timemap', # use the URL to the github repo
    keywords = ['org-mode', 'todotxt', 'todoapp'],
    classifiers = [],
)